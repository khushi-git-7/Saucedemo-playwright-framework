"""The landing page in front of the dashboard on GitHub Pages.

`python -m utils.site` renders `site/index.html` from the same run history the
dashboard reads. Presentation only: nothing in here is imported by the tests
or the dashboard.
"""
